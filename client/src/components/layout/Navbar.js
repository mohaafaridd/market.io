import React from 'react';
import PropTypes from 'prop-types';

const Navbar = props => {
  return (
    <nav>
      <span>Market</span>
    </nav>
  );
};

Navbar.propTypes = {};

export default Navbar;
